import { FC } from "react";
import { Layout } from "../../components/Layout";

const Alertas: FC = () => {
    return (
        <Layout>
            <h1>Alertas</h1>
        </Layout>
    );
}

export default Alertas