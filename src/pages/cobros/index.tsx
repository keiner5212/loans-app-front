import { FC } from "react";
import { Layout } from "../../components/Layout";

const Cobros: FC = () => {
    return (
        <Layout>
            <h1>Cobros</h1>
        </Layout>
    );
}

export default Cobros