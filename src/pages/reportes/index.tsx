import { FC } from "react";
import { Layout } from "../../components/Layout";

const Reportes: FC = () => {
    return (
        <Layout>
            <h1>Reportes</h1>
        </Layout>
    );
}

export default Reportes